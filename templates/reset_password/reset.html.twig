{% extends 'base.html.twig' %}
{% block searchbar %}{% endblock %}
{% block body %}
    <div class="container">
        {{ form_start(resetForm) }}
        {{ form_row(resetForm.plainPassword) }}
        <button class="btn btn-primary">{{ "Form.resetPassword.resetButton" | trans }}</button>
        {{ form_end(resetForm) }}
    </div>
{% endblock %}
